import React from 'react';
import {View, Text} from 'react-native';
import {MAIN_GRAY} from '../../../../config/global-styles';

const Announcements = () => (
  <View style={{height: 300}}>
    <Text style={{color: MAIN_GRAY}}>No Announcements</Text>
  </View>
);

export default Announcements;
